# TODO

## Need a way to pull the project without clobbering local CSV files

if modifying locally, there will be a problem pulling the CSV files (local changes)

if i were to use e.g. example files with different names, then include_str probably won't work
