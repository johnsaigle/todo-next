# TODO

## bug: spaces in the CSV entry cause them to be skipped

If the entries in the CSV file have leading/trailing whitespace, it can be trimmed and then populated as normal

## refactor: don't read from cwd; only read from ~/.config/todo-next

## tests: add unit tests to ensure basic functionality works
